default:
  image: mcr.microsoft.com/dotnet/sdk:8.0

stages:
  - build
  - test

variables:
  DOTNET_CLI_TELEMETRY_OPTOUT: "1"
  NUGET_PACKAGES_DIRECTORY: '.nuget'
  #REPORT_PORTAL_CONFIG_PATH: "epam-at-lab-2024.net\\Project.Core\\ReportPortal.config.json"

  
cache:
  key: "$CI_JOB_STAGE-$CI_COMMIT_REF_SLUG"
  paths:
    - './obj/project.assets.json'
    - './obj/*.csproj.nuget.*'
    - '$NUGET_PACKAGES_DIRECTORY'

before_script:
  - apt-get update && apt-get install -y wget gnupg
  - dotnet restore --packages $NUGET_PACKAGES_DIRECTORY

build:
  stage: build
  script:
    - dotnet build --configuration Release
  only:
    - dev

test_chrome:
  stage: test
  script:
    - echo "Running NUnit tests with ReportPortal integration"
    #- dotnet add package ReportPortal.NUnitExtension
    #- cp $REPORT_PORTAL_CONFIG_PATH .
    - echo "Installing Google Chrome..."
    - wget -qO- https://dl.google.com/linux/linux_signing_key.pub | gpg --dearmor -o /usr/share/keyrings/google-chrome-keyring.gpg
    - echo "deb [signed-by=/usr/share/keyrings/google-chrome-keyring.gpg] http://dl.google.com/linux/chrome/deb/ stable main" > /etc/apt/sources.list.d/google-chrome.list
    - apt-get update && apt-get install -y google-chrome-stable
    - dotnet test --configuration Release --no-restore
    
  only:
    - dev